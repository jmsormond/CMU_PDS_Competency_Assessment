<%= form_for(@indicator_resource) do |f| %>
  <% if @indicator_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@indicator_resource.errors.count, "error") %> prohibited this indicator_resource from being saved:</h2>

      <ul>
      <% @indicator_resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :indicator_id %><br>
    <%= f.number_field :indicator_id %>
  </div>
  <div class="field">
    <%= f.label :resource_id %><br>
    <%= f.number_field :resource_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
