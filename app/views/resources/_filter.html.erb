<%= form_for_filterrific @filterrific do |f| %>
  <div>
    Competency
    <%# give the search field the 'filterrific-periodically-observed' class for live updates %>
    <%= f.select(
      :sort_by_competency,
      @filterrific.select_options[:sort_by_competency],
      { include_blank: '- Any -' }
    ) %>
  </div>
  <div>
    Level
    <%= f.select(
      :sort_by_level,
      @filterrific.select_options[:sort_by_level],
      { include_blank: '- Any -' }
    ) %>
  </div>
  <div>
    Category
    <%= f.select(
      :sort_by_category,
      @filterrific.select_options[:sort_by_category],
      { include_blank: '- Any -' }
    ) %>
  </div>
  <div>
    Active
    <%= f.select(
      :sort_active,
      @filterrific.select_options[:sort_active]
    ) %>
  </div>
  <div>
    <%= link_to(
      'Reset filters',
      reset_filterrific_url,
    ) %>
  </div>
  <%# add an automated spinner to your form when the list is refreshed %>
  <%= render_filterrific_spinner %>
<% end %>

<%= render( partial: 'resources/list', locals: { resources: @resources } ) %>